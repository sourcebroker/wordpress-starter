{
  "name": "sourcebroker/wordpress-starter",
  "type": "project",
  "license": "MIT",
  "description": "WordPress boilerplate installed in root with bedrock style configuration (.env / environments)",
  "config": {
    "preferred-install": "dist",
    "process-timeout": 99999,
    "platform": {
      "php": "7.3"
    }
  },
  "require": {
    "symfony/dotenv": "^4.0 || ^5.0",
    "oscarotero/env": "^1.2",
    "roots/wp-config": "1.0.0",
    "roots/wp-password-bcrypt": "^1.0",

    "sourcebroker/deployer-extended": "dev-master as 13.0.1",
    "sourcebroker/deployer-extended-wordpress": "dev-master",

    "wp-cli/core-command": "^2.0",
    "wp-cli/db-command": "^2.0"
  },
  "minimum-stability": "dev",
  "prefer-stable": true,
  "scripts": {
    "post-create-project-cmd": [
      "php -r \"copy('config/.env.example', 'config/.env');\"",
      "test ! -e \"wp-admin\" && ./vendor/bin/wp core download"
    ],
    "install-wp": [
      "export $(grep WP_HOME config/.env | xargs) && ./vendor/bin/wp core install --url=$WP_HOME --title=Example --admin_user=admin --admin_password=joh316 --admin_email=admin@example.com --skip-email --skip-plugins --skip-themes"
    ]
  }
}
