{
  "config": {
    "preferred-install": "dist",
    "process-timeout": 99999,
    "platform": {
      "php": "7.3"
    }
  },
  "require": {
    "symfony/dotenv": "^4.0 || ^5.0",
    "oscarotero/env": "^1.2",
    "roots/wp-config": "1.0.0",
    "roots/wp-password-bcrypt": "^1.0",

    "sourcebroker/deployer-extended": "dev-master as 13.0.1",
    "sourcebroker/deployer-extended-wordpress": "dev-master",

    "wp-cli/core-command": "^2.0",
    "wp-cli/db-command": "^2.0"
  },
  "minimum-stability": "dev",
  "prefer-stable": true,
  "scripts": {
    "install-wp": [
      "export $(grep WP_HOME config/.env | xargs) && ./vendor/bin/wp core download && ./vendor/bin/wp core install --url= --title=Example --admin_user=admin --admin_password=joh316 --admin_email=admin@localhost --skip-email --skip-content"
    ]
  }
}
